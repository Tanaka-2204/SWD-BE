spring.application.name=loyalty-system
server.port=8080

# ======== 1. CẤU HÌNH POSTGRESQL (LOCAL) ========
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ======== 2. CẤU HÌNH HIBERNATE (JPA) ========
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# ======== 3. CẤU HÌNH BẢO MẬT (AWS COGNITO) ========
# 🔥 QUAN TRỌNG: Cấu hình này dùng để xác thực JWT token
# Nó sẽ tự động đọc các biến AWS_... từ file .env của bạn
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://cognito-idp.${AWS_REGION}.amazonaws.com/${AWS_COGNITO_USER_POOL_ID}

# ======== 4. LOGGING ========
logging.level.root=INFO
logging.level.com.example.demo=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# ======== 5. FILE UPLOAD ========
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true
server.servlet.encoding.force-request-encoding=true
server.servlet.encoding.force-response-encoding=true
server.tomcat.uri-encoding=UTF-8
springdoc.swagger-ui.tags-sorter=alpha

# ======== 6. CLOUDINARY CONFIG ========
CLOUDINARY_URL=cloudinary://${CLOUDINARY_API_KEY}:${CLOUDINARY_API_SECRET}@dsvjevcp9
CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
CLOUDINARY_URL=cloudinary://${CLOUDINARY_API_KEY}:${CLOUDINARY_API_SECRET}@dsvjevcp9