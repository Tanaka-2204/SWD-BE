# ====================================================
# LOYALTY SYSTEM - DOCKER PROFILE
# ====================================================

# --- POSTGRESQL (Docker Container) ---
spring.datasource.url=jdbc:postgresql://loyalty-postgres:5432/loyaltysystem_kzfr
spring.datasource.username=loyaltysystem_kzfr_user
spring.datasource.password=LDPaZKe2gZJm3ASGhHWQ8BC4HAZGPfGL
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# --- JPA/HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# --- LOGGING ---
logging.level.root=INFO
logging.level.com.example.demo=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
